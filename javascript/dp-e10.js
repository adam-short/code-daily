let args = process.argv.slice(2);

function int2day (i) {
  const days = ["Monday", "Tuesday", "Wednesday", "Thursday", "
                Friday", "Saturday", "Sunday"];
  return days[i];
}

function daysIn(month) {
  switch(month) {
    case 1
  }
}

const day = args[0];
const month = args[1];
const year = args[2];

if (day == undefined || month == undefined || year == undefined) {
  console.log("ERROR: Missing a day, month or year.");
} else {
  // day -> month -> year;
  //
}


// TODO
